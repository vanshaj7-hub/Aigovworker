# Copy to keystore.properties and fill in with your own signing key.
# keystore.properties is gitignored; never commit real credentials.
RELEASE_STORE_FILE=my-release.keystore
RELEASE_STORE_PASSWORD=change-me
RELEASE_KEY_ALIAS=my-alias
RELEASE_KEY_PASSWORD=change-me
